<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-inherit="web.WebClient" t-inherit-mode="extension" owl="1">
        <!-- The school app uses the native Odoo NavBar and ActionContainer so
             navigation and the sidebar work exactly like every other module.
             No custom shell/wrapper is injected here. -->
    </t>
</templates>
